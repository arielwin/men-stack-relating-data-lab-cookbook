<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>
        <%= user.username %>'s Pantry
    </h1>
    <% if(user.foods.length===0) { %>
        <h3>This User's Pantry is Empty</h3>
    <% } else {%>
        <ul>
            <% user.foods.forEach((food) =>{ %>

                <li>
                    Name: <%=food.name%>
                    Quantity: <%=food.quantity%>
                    Expiration Date: <%=food.expirationDate%>
                </li>
                   
           <% }) %>
        </ul>
   <% } %>
</body>
</html>